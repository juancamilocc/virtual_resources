trigger: none

pool:
  vmImage: 'ubuntu-latest'

stages:

- stage: Hello
  displayName: 'Second pipeline'
  jobs:
    - job: Greeting
      displayName: 'Second pipeline'
      steps:
        - task: Bash@3
          inputs:
            targetType: 'inline'
            script: |
              echo "This is the second pipeline execution"

              sleep 30